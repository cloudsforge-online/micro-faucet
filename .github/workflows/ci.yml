name: CI

# The whole file. Every service in the estate calls the ONE reusable workflow in micro-org rather
# than carrying its own copy — the measured target in docs/ecosystem/03 §5 is zero bespoke CI files.
# If this repository ever needs a job of its own, that is the signal the reusable workflow needs an
# input, not that this repository needs a fork of it.
#
# `service-ci.yml` typechecks, runs the suite against a real Postgres `ci_test` container, and FAILS
# the build if the database-backed suite skipped — which is why `src/testsupport.ts` reads
# `FAUCET_TEST_DATABASE_URL` spelled exactly as the workflow exports it. It also enforces the estate
# rules (one database, no cross-service imports, the three health endpoints, no unleased timers)
# that repository walls used to enforce for free.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: cloudsforge-online/micro-org/.github/workflows/service-ci.yml@main
    with:
      service: faucet
      database-env-var: FAUCET_DATABASE_URL
      port: 4013
      # The image startup smoke test boots the real service, and `env.ts` validates its
      # configuration at import — so the container needs the variables it declares, not just a
      # database, or it exits before it serves /livez.
      #
      # These are CI-only NON-secrets. Three of them deserve a note:
      #
      #   FAUCET_TOKEN=@generate asks service-ci.yml for a fresh `openssl rand -base64 48` per run,
      #   thrown away afterwards. It used to read `ci-only-not-a-real-token-0000000000000`, which
      #   @cloudsforge/secrets now refuses — `ci-only` and `not-a-real` are both placeholder markers,
      #   and the guard has no CI exemption on purpose (micro-org #142), because an escape hatch is
      #   reached for in the same hurry that produced the defect. CI does not need a WEAK secret, it
      #   needs A secret, so it generates a real one per run rather than committing a high-entropy
      #   literal a future reader could mistake for a key.
      #
      #   CUSTODY_TOKEN is left as a plain throwaway string, and that is not an omission. It is the
      #   one secret this service does NOT hold to a shape: the estate feeds it a 669-character JWT
      #   because custody's gate wants an identity token and this service has no
      #   ServiceTokenProvider, and every guard in @cloudsforge/secrets refuses a JWT by name. See
      #   the block on it in src/env.ts. It buys nothing here either: CUSTODY_URL points at a port
      #   with nothing on it, and custody is deliberately not dialled at boot — a signature is only
      #   needed when there is something to sign.
      #
      #   FAUCET_RPC_URL likewise points at nothing, and the smoke test still passes, because a
      #   node that cannot be reached at boot is a WARNING and a soft readiness probe rather than a
      #   fatal error. `src/index.ts` says why at length: an unreachable node cannot cause a
      #   wrong-chain signature, since the chain id in a signature comes from the pinned package and
      #   is bound a third time by custody from the address's own row. A node that DISAGREES is
      #   still fatal, and that is what the check is for.
      #
      # There is no key here, and there is no variable that would accept one — the funding key is
      # micro-custody's. `src/env.test.ts` proves that by absence.
      smoke-env: |
        FAUCET_TOKEN=@generate
        IDENTITY_JWKS_URL=http://127.0.0.1:4001/.well-known/jwks.json
        IDENTITY_ISSUER=http://127.0.0.1:4001
        FAUCET_RPC_URL=http://127.0.0.1:8545
        CUSTODY_URL=http://127.0.0.1:4005
        CUSTODY_TOKEN=ci-only-not-a-real-token-0000000000000
        FAUCET_FUNDING_ADDRESS=0x00000000000000000000000000000000000000fa
        FAUCET_CUSTODY_USER_ID=cloudsforge:faucet
        FAUCET_CUSTODY_ORDER_ID=faucet:ember:testnet
    secrets:
      # @cloudsforge/* is resolved by link:../runtime and link:../contracts, so CI checks out
      # micro-runtime and micro-contracts as siblings — both private, so the workflow needs a
      # contents:read token.
      estate_token: ${{ secrets.ESTATE_READ_TOKEN }}
    permissions:
      contents: read
      packages: read

  hygiene:
    uses: cloudsforge-online/micro-org/.github/workflows/secret-hygiene.yml@main
    permissions:
      contents: read

  # ──────────────────────────────────────────────────────────────────────────────────────────────
  # THE IMAGE, PUBLISHED. `cfctl release` writes a manifest naming ghcr.io/cloudsforge-online/<repo>
  # at this repository's package.json version (micro-org tools/cfctl.ts, image name derived
  # at tools/registry.ts); `cfctl release --verify` runs `docker manifest inspect` on every
  # entry (cfctl.ts); and deploy/scripts/release-deploy.sh refuses to change anything
  # until all of them pull. Until this job existed CI built this image and threw it away, so that
  # gate rejected every service in the estate and the manifest path could not be used at all.
  #
  # `packages: write` IS GRANTED HERE rather than asked for inside the shared CI workflow, and that
  # is forced rather than stylistic: a called workflow's GITHUB_TOKEN can only be maintained or
  # reduced, never elevated, so a reusable workflow asking for a permission its caller withheld
  # fails the whole run at STARTUP — before a step runs, on pull requests as well as on main.
  # Measured on micro-org: run 30900277471 (caller capped at `packages: read`) ended in
  # startup_failure; run 30900280323 (same workflow, caller granted `packages: write`) succeeded.
  # That is also why `push: false` is correct and stays in the shared workflow's image job — that
  # job is a GATE and needs `load:` so it can boot the image and read its health endpoint. This is
  # the producer. Different purposes, so different jobs.
  # ──────────────────────────────────────────────────────────────────────────────────────────────
  publish:
    needs: [ci, hygiene]
    # A green push to main and nothing else. An image tag a release manifest pins has to name
    # something that passed, on the branch it will be deployed from.
    if: >-
      github.event_name == 'push' && (github.ref == 'refs/heads/main' ||
      startsWith(github.ref, 'refs/heads/release/'))
    uses: cloudsforge-online/micro-org/.github/workflows/publish-image.yml@main
    with:
      kind: service
    secrets:
      # The same sibling checkouts the Dockerfile's named build contexts need: the image cannot be
      # built without micro-runtime and micro-contracts beside it.
      estate_token: ${{ secrets.ESTATE_READ_TOKEN }}
    permissions:
      contents: read
      packages: write
